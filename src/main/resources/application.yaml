spring:
  application:
    name: ai-interviewer

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: gemma2:2b # 본인이 다운로드한 Ollama 모델명 입력
          temperature: 0.7 # 답변의 창의성 조절 (0.7 내외가 적당)